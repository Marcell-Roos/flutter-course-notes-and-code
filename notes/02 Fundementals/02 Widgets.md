Flutter UI is built with widgets by combining and nesting them, the `runApp` function takes a Widget tree

Widgets are often nested, creating a widget tree

You can use Flutter's packaged widgets or build your own custom widgets

"Container" does not support cont, nor can it have a const parent